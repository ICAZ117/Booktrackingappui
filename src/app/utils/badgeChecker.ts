import type { Book, ReadingSession } from "../contexts/BooksContext";

export function checkBadges(
  _books: Book[],
  _readingSessions: ReadingSession[],
  _earnedBadges: string[],
): string[] {
  return [];
}
